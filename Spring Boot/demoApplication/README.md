This is a simple Spring Boot application that uses my custom greeting-starter library

Before you start it you need to run `mvn clean install` under the greeting package so this custom library is compiled ant placed under your local .m2 folder 