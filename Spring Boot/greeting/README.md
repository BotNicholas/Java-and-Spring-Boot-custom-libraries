This is the parent pom for all the modules (core, autoconfiguration and starter).

It is used to build all the modules with a single `mvn clean install` on parent pom.

Also it's useful for centralised dependencies management.